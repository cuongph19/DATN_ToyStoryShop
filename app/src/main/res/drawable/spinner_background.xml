<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Bo góc cho Spinner -->
    <corners android:radius="8dp"/>

    <!-- Màu nền của Spinner -->
    <solid android:color="#9AFFFFFF"/> <!-- Màu xanh đậm -->

    <!-- Đường viền cho Spinner -->
    <stroke
        android:width="1dp"
        android:color="@color/gray"/> <!-- Đường viền trắng -->

    <!-- Padding bên trong Spinner -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp"/>
</shape>
